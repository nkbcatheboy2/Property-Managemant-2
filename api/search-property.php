<?php require_once __DIR__.'/../includes/auth.php';header('Content-Type: application/json');$q=trim($_GET['q']??'');$s=db()->prepare('SELECT scheme_name,property_no,property_code,address,category,status FROM properties WHERE scheme_name LIKE ? OR property_no LIKE ? OR property_code LIKE ? OR address LIKE ? LIMIT 20');$like='%'.$q.'%';$s->execute([$like,$like,$like,$like]);echo json_encode($s->fetchAll());